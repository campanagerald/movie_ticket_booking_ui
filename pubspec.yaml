name: movie_ticket_booking_ui
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^2.5.0
  cupertino_icons: ^1.0.0
  dio: ^3.0.10
  flutter_riverpod: ^0.12.4
  flutter_staggered_grid_view: ^0.3.4
  intl: ^0.16.1
  state_notifier: ^0.6.0
  shimmer: ^1.1.2
  animations: ^1.1.2
  freezed_annotation: ^0.12.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed: ^0.12.7
  json_serializable: ^3.5.1

flutter:
  assets:
    - assets/images/

  uses-material-design: true
